# Service offering example

Creates a root-scoped service offering with `dedicated_cpu`. Destroy calls `DELETE /service-offerings/{id}`.

Requires root credentials and **no** `tenant_id` on the provider.
